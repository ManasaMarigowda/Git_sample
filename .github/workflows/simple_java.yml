name: My First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: check java install or not 
        run: java --version
      - name: install java
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: 'temurin'
